# Active Alignment Register

This file contains only decisions that are still partial, unresolved, or blocked. Stable decisions belong in canonical specs or status-bearing ADRs and must not be copied here.

Status values:

```text
partial       direction exists but implementation-critical detail is missing
unresolved    user discussion is required
blocked       external evidence, feasibility work, or implementation is required first
```

## P0: blocks safe implementation

| Area | Status | Decision still required | Canonical home after decision |
| --- | --- | --- | --- |
| Public Gmail verification evidence | blocked | The local-app-only OAuth boundary, independent attachment/body capability switches, provider-fingerprint-bound mailbox consent, re-consent when the AI recipient/disclosure changes, direct configured-AI transfer disclosure, and no repeated rule-level prompt are accepted. Before public Gmail promotion, provision and validate the `cancan.money` production Desktop OAuth client, provider-specific retention wording, Limited Use compatibility, website/privacy contacts, and Google's required verification against the running multi-mailbox flow | `docs/specs/0003-gmail-collector.md`, `docs/specs/0007-first-run-onboarding.md`, `docs/specs/0018-app-updates-open-source-release.md` |

## P1: required before the affected implementation slice

| Area | Status | Decision still required | Canonical home after decision |
| --- | --- | --- | --- |
| Public release provisioning | blocked | `CanCan`/`cancan`/`cancan.money`, Apache-2.0, macOS 14, founding-owner governance and release approval, owner-held Apple identity, CI-held Tauri updater secret plus encrypted offline recovery, immutable published artifacts, and one `0.x Preview` channel with an off-by-default update switch are accepted. Provision and verify the domain, intended `support@`/`security@` contacts, Cloudflare project, Apple identity, updater key/recovery, dual-architecture packaging, production Google project, and release environments before public promotion | `docs/specs/0018-app-updates-open-source-release.md` |
| Backup operations and portability | unresolved | Manual/automatic schedule, failure UX, Command Center status, export formats, and restore-migration detail | `docs/specs/0009-backup-restore-versioning.md`, `docs/specs/0015-job-engine-error-model.md` |
| Statement coverage AI analysis and actions | partial | A separate future feature, one capability-specific API under the unified Phase 2 backend interface, advisory AI result, no daemon, source/account/period/processing changes marking the scope due, once-per-local-day startup/unlock catch-up, one Money Source/account/document-type scope per invocation, bounded structured input plus at most three in-scope documents, two pages per document, 8,000 extracted characters total, and one follow-up snippet request, plus exact-period `No statement this period` and user-dated `Remind later` actions are accepted. Decide provider/model retention and cost plus confidence/explanation rules | `docs/specs/0015-job-engine-error-model.md`, `docs/specs/0017-evidence-documents-source-ux.md`, `docs/specs/0019-ai-capability-platform-and-cli.md` |
| Repayment allocation and FX | partial | Future repayment review supports partial, grouped, and cross-currency cases with no arbitrary tolerance. AI proposes an exact allocation and concise explanation for one-click user acceptance; the host validates the full allocation and progressive disclosure retains exact rows/evidence. Decide allocation units, FX evidence/rate ownership, explicit fees/remainders, and search bounds; the current exact same-currency seven-day recommendation remains the only implemented rule | `docs/specs/0005-review-and-commit-policy.md`, `docs/specs/0013-ledger-assets-valuation.md` |

## P2: deliberately deferred, does not block the core MVP slices

| Area | Status | Decision still required | Canonical home after decision |
| --- | --- | --- | --- |
| Usage cadence and manual money | unresolved | Retention workflow, physical cash, and manual asset/liability scope | future focused product spec after the decision |
| Host error observability | partial | Runtime errors reach the renderer as stable static codes; only blocking-task join failures currently reach stderr, while domain error causes (keyring, database, sidecar) are discarded and keyring store traits return `Result<_, ()>`. The local-only redacted 30-day log boundary is accepted; decide typed store-trait errors and prove the durable local log implementation/export path without exposing sensitive causes. Long OCR/extraction/sidecar work outside the shared store mutex and short version-validated read/write lock ownership are accepted; the `ManualImportStore` per-aggregate impl split remains a structural follow-up, not an unresolved concurrency decision | `docs/specs/0001-repo-structure.md`, `docs/specs/0015-job-engine-error-model.md` |
| AI capability platform and assistant scope | partial | One Phase 2 host-owned typed capability/service registry is accepted as the unified backend interface for the renderer, in-app AI, internal scheduled jobs, full first-party AI CLI, and strict external-agent tools. The target catalog covers all existing user-facing read outcomes and host-validated edit/link/commit/delete/restore mutations through purpose-specific capabilities; Tauri/in-process/socket/tool layers are adapters, and a new caller/transport never creates a duplicate capability or business implementation. External access is off by default over a user-only local socket; Desktop remains the sole Vault and mutation authority. Decide in-app assistant history/settings, external approval persistence/audit retention, and remaining feature-specific bounds | `docs/specs/0006-command-center-ui.md`, `docs/specs/0019-ai-capability-platform-and-cli.md` |
| Optional hosted AI service | partial | A paid CanCan-hosted relay is allowed later behind the existing provider-adapter/capability boundary, but its identity, payment, entitlement, minimal-state, retention, and privacy contracts are deferred. MVP remains direct BYO endpoint/model/API key | future focused service and release spec |
| macOS Finder Share intake | partial | Phase 2 adds native Finder `Share > CanCan` as a thin adapter over the existing Add/capture path. When Desktop is closed or locked, the extension copies the selected files into a bounded App Group handoff area for import after unlock; this transient staging is not a second parser/job queue and never receives the Vault key. Decide the exact extension/service target, at-rest protection and cleanup/expiry contract for staged plaintext, multi-file/time limits, cancellation/completion copy, entitlements, and packaged Developer ID behavior | `docs/specs/0017-evidence-documents-source-ux.md`, `docs/specs/0018-app-updates-open-source-release.md` |
| Post-MVP extensions | blocked | Windows desktop is accepted for Phase 2 but still needs exact OS/architecture, installer/updater, signing, secret-store, filesystem, and validation contracts. A thin native iOS Share Extension/containing-app intake direction is accepted: a disposable local Xcode slice must choose the transport/encryption/offline/pairing/native-target boundary, while a later authorized mobile release slice owns signing, TestFlight, and App Store evidence. Mobile ledger/multi-device sync, API connectors, valuation providers, tax reporting, and parser sharing remain gated on the core loop | `docs/specs/0018-app-updates-open-source-release.md` plus future roadmap after MVP evidence exists |
